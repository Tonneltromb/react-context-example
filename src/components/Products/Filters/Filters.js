import React from 'react';

import PropTypes from 'prop-types';

import './Filters.scss'

const Filters = (props) => {
    return (
        <div className='Filters'>

        </div>
    );
};

Filters.propTypes = {

};

export default Filters;
